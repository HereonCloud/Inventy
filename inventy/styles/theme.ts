import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    allVariants: {
      color: "#F6F6F2",
      fontFamily: "TiltNeon",
    },
  },
  palette: {
    primary: { main: "#388087" },
    secondary: { main: "#F6F6F2" },
  },
});
